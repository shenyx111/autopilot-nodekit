# Example project prompt

基于 autopilot-nodekit 包里的逻辑，完成以下任务：

为一个示例科研自动化项目建立可验证、可恢复、可审计的 loop。请先生成 PROJECT_SPEC、GOAL_CONTRACT 和任务 DAG；缺少关键设置时先问我；使用 balanced gate mode 和 smoke task scale；不要提交真实计算任务，不要访问私有数据。
