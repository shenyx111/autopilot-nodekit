# Security

Autopilot NodeKit is experimental local automation software. Treat it as a control-plane tool, not a security boundary.

Do not store API keys, credentials, private datasets, model weights, Slurm outputs, or real run logs in public issues or commits.

For sensitive reports, use GitHub private security advisories if available. For normal bugs, open an issue with redacted logs and minimal reproduction steps.
