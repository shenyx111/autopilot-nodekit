# Bundle notes — Autopilot NodeKit v0.8.0

Use `smart-start` as the only recommended first command. It prevents silent defaults for gate mode and task scale, generates the project files, and blocks for concise questions only when required.

Validated bundle checks are listed in the final assistant response that produced this zip.
